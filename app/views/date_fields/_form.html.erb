<%= form_with(model: date_field, local: true) do |form| %>
  <% if date_field.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(date_field.errors.count, "error") %> prohibited this date_field from being saved:</h2>

      <ul>
      <% date_field.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :date1, 'Last Update Date' %>
    <%= form.date_select :date1, id: :date_field_date1 %>
  </div>

  <div class="actions">
    <%= form.submit "update", class: 'btn btn-primary'%>
  </div>
<% end %>
