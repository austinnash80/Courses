<div class="well text-center">
  <h1 class=""><big>Sequoia Customer Marketing Dashboard</big></h1>
  <br>
  <%= link_to 'Postcards', rc_marketing_s_customers_path(page: 'postcards'), class: 'btn btn-lg btn-success' %>
</div>
<h3 class="text-center">All states included - EXP is based on membership purchase date, not CPE due date</h3>
<br>
<% if params['day'].present? %>
  <%= render partial: 'rc_marketing_stage' %>
<% else %>
  <%= render partial: 'rc_marketing_buttons' %>
<% end %>
